\chapter{\IfLanguageName{dutch}{Experimentopzet}{Experimental Setup}}%
\label{ch:experimentopzet}

Om de werking van RustProv te evalueren, werd een testomgeving opgezet die aansluit bij een realistische onderwijscontext. Daarbij werd gekozen voor twee afzonderlijke scenario's: een Windows-omgeving en een Linux-omgeving. De Windowsomgeving is gebaseerd op een opstelling uit het vak Windows Server 2, terwijl de Linuxomgeving gebaseerd is op een opstelling uit het vak Software Development Project (SDP). Op die manier sluiten de experimenten aan bij bestaande en relevante praktijksituaties binnen de opleiding.

De testomgeving wordt gebruikt om RustProv te evalueren en te vergelijken met Vagrant als bestaande provisioningoplossing. Voor beide tools worden dezelfde virtuele machines, besturingssystemen, hardwareconfiguraties, netwerkinstellingen en provisioningscripts gebruikt. Hierdoor kunnen beide oplossingen onder zo gelijk mogelijke omstandigheden worden vergeleken.

De keuze voor deze opzet maakt het mogelijk om RustProv te testen in scenario's met één of meerdere virtuele machines, onderlinge afhankelijkheden en afzonderlijke provisioningscripts per machine. Op die manier kan niet alleen worden nagegaan of de tool technisch correct werkt, maar ook of RustProv bruikbaar is in een onderwijs- en labo-omgeving. De scenario's worden bovendien zo opgesteld dat zowel eenvoudige als complexere configuraties kunnen worden geëvalueerd.

De experimenten worden uitgevoerd voor één Linux-VM, meerdere Linux-VM's, één Windows-VM en meerdere Windows-VM's. Voor elk scenario wordt een overeenkomstige configuratie voorzien voor RustProv en Vagrant. De resultaten van deze experimenten worden in het volgende hoofdstuk weergegeven en besproken. Ook de manier waarop de verschillende tijdsmetingen worden uitgevoerd, wordt in het volgende hoofdstuk toegelicht.

\section{Windows-omgeving}

Voor het Windows-scenario werd een omgeving uitgewerkt die gebaseerd is op een opstelling uit het vak Windows Server 2. Deze omgeving bevat meerdere samenwerkende Windows-systemen binnen één netwerkstructuur. Ze wordt gebruikt om na te gaan of RustProv zowel een afzonderlijke Windows-VM als een complexere opstelling met meerdere Windows-systemen kan verwerken.

De Windowsomgeving wordt ook met Vagrant opgebouwd. Hierbij worden dezelfde VM-rollen, provisioningscripts en netwerkconfiguratie gebruikt als bij de RustProv-opstelling. Op die manier wordt een vergelijkingsbasis gecreëerd waarbij het verschil tussen beide oplossingen zo weinig mogelijk wordt beïnvloed door verschillen in de testomgeving.

\subsection{Eén virtuele machine}

Als eerste Windows-testgeval wordt één afzonderlijke Windows 10-VM gebruikt. Deze test heeft als doel te verifiëren of RustProv een enkele Windows-machine correct kan opstarten en provisionen. In tegenstelling tot de meerledige Windows-opstelling wordt in dit scenario een eenvoudig PowerShell-script gebruikt, zodat de basiswerking van de Windows-provisioning afzonderlijk kan worden gecontroleerd.

\paragraph{Provisioningscript}

Het provisioningscript wordt gebruikt om de Windows-client voor te bereiden en de vereiste beheertools te installeren. Het script schrijft eerst een tekstbericht naar de console. Vervolgens wordt een bestand aangemaakt waarin een tweede tekstbericht wordt opgeslagen. Ten slotte wordt de inhoud van dit bestand opnieuw weergegeven op de console. Op basis van deze uitvoer kan worden gecontroleerd of het script effectief op de juiste virtuele machine werd uitgevoerd.

\begin{lstlisting}[language=bash,caption={Provisioningscript voor één Windows-VM.},label={lst:bpwindows}]
    Write-Host "Hello, World!"
    Write-Host "Hello, World from a file!" > $HOME\hello.txt
    Get-Content $HOME\hello.txt
\end{lstlisting}

Voor het experiment met één Windows-VM werd dezelfde configuratie opgesteld voor RustProv en Vagrant. Beide gebruiken dezelfde Windows 10-image, VM-naam, hardware-instellingen, netwerkadapter en het provisioningscript \texttt{00-hello.ps1}. De configuraties worden weergegeven in Listing~\ref{lst:rustprov-windows-config-one} en Listing~\ref{lst:vagrant-windows-config-one}.

\begin{lstlisting}[language=bash,caption={RustProv-configuratie voor één Windows-VM.},label={lst:rustprov-windows-config-one}]
    [windowsbp]
    image = "windowsServer2025"
    cores = 4
    memory = 4096
    script = ["00-hello.ps1"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
\end{lstlisting}

\begin{lstlisting}[language=ruby,caption={Vagrant-configuratie voor één Windows-VM.},label={lst:vagrant-windows-config-one}]
    Vagrant.configure("2") do |config|
    windows.vm.box = "gusztavvargadr/windows-server-2025-standard-core"
    windows.vm.box_version = "2601.0.0"
    windows.vm.box_version = "2511.0.0"
    windows.vm.hostname = "windowsbp"
    windows.vm.communicator = "winrm"
    windows.winrm.username = "vagrant"
    windows.winrm.password = "vagrant"
    windows.vm.network "public_network",
    bridge: "Realtek Gaming 2.5GbE Family Controller"
    windows.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    windows.vm.provision "shell", path: "scripts/00-hello.ps1"
    end
    end
\end{lstlisting}

\subsection{Meerdere virtuele machines}

Daarnaast wordt ook een Windows-opstelling met meerdere virtuele machines getest. Deze configuratie sluit aan bij een Windows Server-omgeving waarin meerdere systemen gelijktijdig worden ingezet, waaronder domeincontrollers en clients. Het gebruik van meerdere Windows-VM's maakt het mogelijk om RustProv te evalueren in een scenario met meerdere nodes, afzonderlijke scripts en een gezamenlijke uitvoeringscontext.

De drie VM's hebben elk een eigen rol binnen de testomgeving. De eerste VM wordt gebruikt als primaire domeincontroller. De tweede VM wordt gebruikt als secundaire domeincontroller of application server. De derde VM wordt gebruikt als Windows-client die toetreedt tot het domein. De configuratie van deze VM's is onderling verbonden, omdat de clients en secundaire server gebruikmaken van het Active Directory-domein dat op de eerste VM wordt ingericht.

Voor elke Windows-VM wordt een apart provisioningscript voorzien. Daardoor krijgt elke machine een eigen configuratie en rol binnen de bredere testomgeving. Deze aanpak maakt het mogelijk om te beoordelen of RustProv de juiste scriptstappen op de juiste machine uitvoert en of meerdere Windows-systemen correct volgens de gewenste volgorde kunnen worden verwerkt.

Dezelfde drie VM-rollen, scripts en netwerkadressen worden ook in de Vagrant-configuratie opgenomen. Hierdoor worden de RustProv- en Vagrant-opstelling inhoudelijk gelijk gehouden.

\subsubsection{Windows-script 1: Active Directory installatie}

Dit script configureert Windows Server 1 en bereidt de machine voor op de rol van primaire domeincontroller. Het script stelt eerst een statisch IP-adres en DNS in, installeert vervolgens de rollen voor Active Directory Domain Services (AD DS) en DNS, en promoot de server tot domeincontroller door een nieuw AD-forest aan te maken. De configuratie hiervan wordt weergegeven in Listing~\ref{lst:win-ad-install}.

\begin{lstlisting}[language=bash,caption={Windows-script 1: installatie van Active Directory.},label={lst:win-ad-install}]
    $domainName = "WS2-25-jens.hogent"
    $safeModePassword = "1243Vagrant@"
    $staticIP = "192.168.0.10"
    $prefixLength = 24
    $interfaceAlias = "Ethernet 2"
    
    Write-Host "Setting Static IP Address for $interfaceAlias..."
    
    New-NetIPAddress -InterfaceAlias $interfaceAlias -IPAddress $staticIP -PrefixLength $prefixLength -ErrorAction Stop
    Write-Host "IP address set to $staticIP/$prefixLength."
    
    Write-Host "Setting DNS Server address..."
    Set-DnsClientServerAddress -InterfaceAlias $interfaceAlias -ServerAddresses "127.0.0.1"
    Write-Host "DNS server set to 127.0.0.1."
    
    
    Write-Host "Installing Active Directory and DNS roles..."
    Install-windowsfeature -name AD-Domain-Services,DNS -IncludeManagementTools
    Write-Host "AD and DNS roles installed."
    
    $securePassword = ConvertTo-SecureString $safeModePassword -AsPlainText -Force
    Write-Host "Creating new AD Forest with domain name $domainName..."
    
    
    Install-ADDSForest -DomainName $domainName -DomainMode "Win2025" -ForestMode "Win2025" -InstallDns:$true -Force:$true -SafeModeAdministratorPassword $SecurePassword 
    Write-Host "AD Forest created successfully. The server will now reboot to complete the installation."
\end{lstlisting}

\subsubsection{Windows-script 2: Active Directory configuratie}

Dit script voert de verdere configuratie van de domeincontroller uit nadat Active Directory is geïnstalleerd. Het maakt eerst de benodigde organisatorische eenheden (OUs) aan, zoals AdminAccounts, Finance, Human Resources en IT. Vervolgens worden de gebruikersaccounts aangemaakt en in de juiste OUs geplaatst. Gebruikers die als beheerder zijn gemarkeerd, worden ook toegevoegd aan de Domain Admins-groep. De configuratie hiervan wordt weergegeven in Listing~\ref{lst:win-ad-config}.

\begin{lstlisting}[language=bash,caption={Windows-script 2: configuratie van Active Directory.},label={lst:win-ad-config}]
    Write-Host "Waiting for Active Directory to become responsive..."
    $timeout2 = New-TimeSpan -Minutes 10
    $stopwatch2 = [System.Diagnostics.Stopwatch]::StartNew()
    
    while ($stopwatch2.Elapsed -lt $timeout2) {
        try {
            Get-ADDomain -ErrorAction Stop | Out-Null
            Write-Host "Active Directory is responsive."
            break
        } catch {
            Write-Host "Waiting for Active Directory to respond... (checked for $([int]$stopwatch2.Elapsed.TotalSeconds) seconds)"
            Start-Sleep -Seconds 5
        }
    }
    
    if ($stopwatch2.Elapsed -ge $timeout2) {
        Write-Error "Active Directory did not become responsive within the $($timeout2.TotalMinutes) minute timeout period."
        exit 1
    }
    $domainName = "WS2-25-jens.hogent"
    $password = "1243Vagrant@" | ConvertTo-SecureString -AsPlainText -Force
    
    $baseDN = ($domainName.Split('.') | ForEach-Object { "DC=$_" }) -join ','
    Write-Host "Base DN calculated as: $baseDN"
    
    $ous = @("AdminAccounts", "Finance", "Human Resources", "IT")
    $users = @(
    @{ Name = "Jens Gosseye"; OU = "AdminAccounts"; IsAdmin = $true },
    @{ Name = "Geert Gossye"; OU = "AdminAccounts"; IsAdmin = $true },
    @{ Name = "Steven Stone"; OU = "Finance"; IsAdmin = $false },
    @{ Name = "Mario Mario"; OU = "Human Resources"; IsAdmin = $false }
    )
    
    Write-Host "Creating OUs..."
    foreach ($ou in $ous) {
        if (-not (Get-ADOrganizationalUnit -Filter "Name -eq '$ou'")) {
            New-ADOrganizationalUnit -Name $ou -Path $baseDN
            Write-Host "- OU '$ou' created."
        } else {
            Write-Host "- OU '$ou' already exists."
        }
    }
    
    Write-Host "Creating Users..."
    foreach ($user in $users) {
        $samAccountName = $user.Name.Replace(" ", ".")
        $userPrincipalName = "$samAccountName@$domainName"
        $ouPath = "OU=$($user.OU),$baseDN"
        
        if (-not (Get-ADUser -Filter "SamAccountName -eq '$samAccountName'")) {
            New-ADUser -Name $user.Name -SamAccountName $samAccountName -UserPrincipalName $userPrincipalName -AccountPassword $password -Enabled $true -Path $ouPath
            Write-Host "- User '$($user.Name)' created in OU '$($user.OU)'."
            if ($user.IsAdmin) {
                Add-ADGroupMember -Identity "Domain Admins" -Members $samAccountName
                Write-Host "  - Added '$($user.Name)' to Domain Admins."
            }
        } else {
            Write-Host "- User '$($user.Name)' already exists."
        }
    }
\end{lstlisting}

\subsubsection{Windows-script 3: DNS-configuratie}

Dit script voert de DNS-configuratie uit op de primaire domeincontroller. Het stelt de DNS-client in op beide netwerkadapters, configureert forwarders naar externe DNS-servers en maakt de benodigde forward en reverse lookup zones aan. Vervolgens worden de A- en PTR-records voor de server toegevoegd en wordt de zone-overdracht geconfigureerd zodat de secundaire DNS-server de zones kan repliceren. De configuratie hiervan wordt weergegeven in Listing~\ref{lst:win-dns-config}.

\begin{lstlisting}[language=bash,caption={Windows-script 3: DNS-configuratie.},label={lst:win-dns-config}]
    $domainName = "WS2-25-jens.hogent"
    $primaryDnsServer = "192.168.0.10"
    $reverseZoneName = "0.168.192.in-addr.arpa"
    $networkId = "192.168.0.0/24"
    
    Set-DnsClientServerAddress -InterfaceAlias "Ethernet 2" -ServerAddresses $primaryDnsServer -ErrorAction SilentlyContinue
    Set-DnsClientServerAddress -InterfaceAlias "Ethernet" -ServerAddresses $primaryDnsServer -ErrorAction SilentlyContinue
    
    Set-DnsServerForwarder -IPAddress "8.8.8.8", "1.1.1.1" -PassThru
    
    if (-not (Get-DnsServerZone -Name $reverseZoneName -ErrorAction SilentlyContinue)) {
        Add-DnsServerPrimaryZone -NetworkID $networkId -ReplicationScope "Domain" -PassThru
        Write-Host "- Reverse Lookup Zone '$reverseZoneName' created."
    } else {
        Write-Host "- Reverse Lookup Zone '$reverseZoneName' already exists."
    }
    
    Write-Host "Creating Forward Lookup Zone..."
    if (-not (Get-DnsServerZone -Name $domainName -ErrorAction SilentlyContinue)) {
        Add-DnsServerPrimaryZone -Name $domainName -ReplicationScope Domain -DynamicUpdate Secure
        Write-Host "- Forward Lookup Zone '$domainName' created."
    } else {
        Write-Host "- Forward Lookup Zone '$domainName' already exists."
    }
    
    if (-not (Get-DnsServerResourceRecord -Name "server1" -ZoneName $domainName -RRType "A" -ErrorAction SilentlyContinue)) {
        Add-DnsServerResourceRecordA -Name "server1" -ZoneName $domainName -IPv4Address $primaryDnsServer
        Write-Host "- Created 'server1' A record."
    } else {
        Write-Host "- 'server1' A record already exists."
    }
    
    if (-not (Get-DnsServerResourceRecord -Name "10" -ZoneName $reverseZoneName -RRType "Ptr" -ErrorAction SilentlyContinue)) {
        Add-DnsServerResourceRecordPtr -Name "10" -ZoneName $reverseZoneName -PtrDomainName "server1.$domainName"
        Write-Host "- Created PTR record for 'server1'."
    } else {
        Write-Host "- PTR record for 'server1' already exists."
    }
    
    
    Set-DnsServerPrimaryZone -Name $domainName -SecureSecondaries "TransferToSecureServers" -SecondaryServers $secondaryDnsServer -PassThru
    
    Write-Host "DNS configuration complete."
\end{lstlisting}

\subsubsection{Windows-script 4: secundaire server}

Dit script configureert Windows Server 2 en voorziet de instellingen die nodig zijn voor de samenwerking met de primaire domeincontroller. Het stelt eerst een statisch IP-adres in en wijzigt de DNS-server naar de primaire domeincontroller. Vervolgens controleert het script of de server reeds lid is van het domein. Indien niet, dan treedt de server toe tot het bestaande Active Directory-domein. De configuratie hiervan wordt weergegeven in Listing~\ref{lst:win-server2}.

\begin{lstlisting}[language=bash,caption={Windows-script 3: toetreden tot Active Directory.},label={lst:win-server2}]
    $domainName = "WS2-25-jens.hogent"
    $adminPassword = "1243Vagrant@"
    
    
    $ip = "192.168.0.20"
    $interface = "Ethernet 2"
    $dns = "192.168.0.10"
    
    $currentIP = Get-NetIPAddress -InterfaceAlias $interface -AddressFamily IPv4 -ErrorAction SilentlyContinue | Where-Object { $_.IPAddress -eq $ip }
    if (-not $currentIP) {
        Write-Host "Setting Static IP Address for Server 2..."
        New-NetIPAddress -InterfaceAlias $interface -IPAddress $ip -PrefixLength 24 | Out-Null
        Write-Host "IP address set to $ip/24."
    } else {
        Write-Host "IP address $ip is already set on $interface."
    }
    
    Write-Host "Setting DNS server to $dns..."
    Set-DnsClientServerAddress -InterfaceAlias $interface -ServerAddresses $dns
    Set-DnsClientServerAddress -InterfaceAlias "Ethernet" -ServerAddresses $dns
    Write-Host "DNS server set."
    
    
    $computerInfo = Get-ComputerInfo
    if ($computerInfo.Domain -ne $domainName) {
        Write-Host "Joining domain $domainName..."
        $credential = New-Object System.Management.Automation.PSCredential("Jens.Gosseye@$domainName", (ConvertTo-SecureString $adminPassword -AsPlainText -Force))
        Add-Computer -DomainName $domainName -Credential $credential
        Write-Host "Joined domain. The server will reboot."
    } else {
        Write-Host "Computer is already joined to the domain '$domainName'."
    }
\end{lstlisting}

\subsubsection{Windows-script 5: Windows-client}

Dit script configureert de Windows-client binnen de meerledige Windows-omgeving. Het stelt eerst een statisch IP-adres in en wijzigt de DNS-server op beide netwerkadapters naar de primaire domeincontroller. Vervolgens wordt de DNS-cache geleegd en wordt gecontroleerd of de client reeds lid is van het domein. Indien niet, dan treedt de client toe tot het bestaande Active Directory-domein. De configuratie hiervan wordt weergegeven in Listing~\ref{lst:win-client}.

\begin{lstlisting}[language=bash,caption={Windows-script 4: Windows-client toetreden tot Active Directory.},label={lst:win-client}]
    Write-Host "Starting Client Domain Join for WS2-25-jens.hogent..."  
    $domainName = "WS2-25-jens.hogent"
    $adminPassword = "1243Vagrant@"
    $user = "Jens.Gosseye@$domainName"
    
    $ip = "192.168.0.15"
    $interface = "Ethernet 2"
    $natAlias = "Ethernet"
    $dns = "192.168.0.10"
    
    $currentIP = Get-NetIPAddress -InterfaceAlias $interface -AddressFamily IPv4 -ErrorAction SilentlyContinue | Where-Object { $_.IPAddress -eq $ip }
    if (-not $currentIP) {
        Write-Host "Setting Static IP Address for Client..."
        New-NetIPAddress -InterfaceAlias $interface -IPAddress $ip -PrefixLength 24 | Out-Null
        Write-Host "IP address set to $ip/24."
    } else {
        Write-Host "IP address $ip is already set on $interface."
    }
    
    Write-Host "Setting DNS server to $dns on both adapters to prevent leakage..."
    Set-DnsClientServerAddress -InterfaceAlias $interface -ServerAddresses $dns
    Set-DnsClientServerAddress -InterfaceAlias $natAlias -ServerAddresses $dns
    Write-Host "DNS servers updated."
    
    Write-Host "Flushing DNS cache so Windows sees the new routing..."
    Clear-DnsClientCache
    Start-Sleep -Seconds 3
    
    $computerInfo = Get-ComputerInfo
    if ($computerInfo.Domain -ne $domainName) {
        Write-Host "Joining domain $domainName..."
        $securePass = ConvertTo-SecureString $adminPassword -AsPlainText -Force
        $credential = New-Object System.Management.Automation.PSCredential($user, $securePass)
        
        Add-Computer -DomainName $domainName -Credential $credential -Force
        
        Write-Host "Joined domain successfully! The machine is rebooting now..."
        Restart-Computer -Force
    } else {
        Write-Host "Computer is already joined to the domain '$domainName'."
    }
\end{lstlisting}

Voor de Windows-experimenten werd dezelfde VM-opstelling beschreven in zowel een TOML-configuratiebestand voor RustProv als een \texttt{Vagrantfile} voor Vagrant. Op die manier wordt ervoor gezorgd dat beide provisioners dezelfde virtuele machines, hardware-instellingen, netwerkadapter en provisioningscripts gebruiken. Bij RustProv werd wel een wachttijd van 360 seconden toegevoegd om te wachten tot Active Directory volledig beschikbaar is. Bij Vagrant gebeurt dit wachten automatisch. De configuraties worden weergegeven in Listing~\ref{lst:rustprov-windows-config} en Listing~\ref{lst:vagrant-windows-config}.

\begin{lstlisting}[language=bash,caption={RustProv-configuratie voor meerdere Windows-VM's.},label={lst:rustprov-windows-config}]
    [windowsad1]
    image = "windowsServer2025"
    cores = 4
    memory = 4096
    script = ["AD-Server1.ps1","wait(360)","ConfigureAD-Server1.ps1","DNS-Server1.ps1"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
    
    
    [windowsad2]
    image = "windowsServer2025"
    cores = 4
    memory = 4096
    script = ["JoinAD-Server2.ps1"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
    
    [windowsclient]
    image = "windows10"
    cores = 4
    memory = 4096
    script = ["JoinAD-Client.ps1"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
    \end{lstlisting}

\begin{lstlisting}[language=ruby,caption={Vagrant-configuratie voor meerdere Windows-VM's.},label={lst:vagrant-windows-config}]
    Vagrant.configure("2") do |config|
    config.vm.communicator = "winrm"
    config.winrm.username = "vagrant"
    config.winrm.password = "vagrant"
    config.vm.define "windowsad1" do |ad1|
    ad1.vm.box = "gusztavvargadr/windows-server-2025-standard-core"
    ad1.vm.box_version = "2601.0.0"
    ad1.vm.hostname = "windowsad1"   
    ad1.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller" 
    ad1.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    ad1.vm.provision "install-ad", type: "shell", path: "scripts/AD-Server1.ps1"    
    ad1.vm.provision "configure-ad", type: "shell", path: "scripts/ConfigureAD-Server1.ps1",reboot: true
    ad1.vm.provision "dns-server", type: "shell", path: "scripts/DNS-Server1.ps1"
    ad1.vm.boot_timeout = 600
    end
    config.vm.define "windowsad2" do |ad2|
    ad2.vm.box = "gusztavvargadr/windows-server-2025-standard-core"
    ad2.vm.box_version = "2601.0.0"
    ad2.vm.hostname = "windowsad2"   
    ad2.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller" 
    ad2.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    ad2.vm.provision "shell", path: "scripts/JoinAD-Server2.ps1"
    ad2.vm.boot_timeout = 600
    end
    config.vm.define "windowsclient" do |client|
    client.vm.box = "gusztavvargadr/windows-10-22h2-enterprise"
    client.vm.box_version = "2511.0.0"
    client.vm.hostname = "windowsclient" 
    client.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller" 
    client.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    client.vm.provision "shell", path: "scripts/JoinAD-Client.ps1"
    client.vm.boot_timeout = 600
    end
    end
\end{lstlisting}




\section{Linux-omgeving}

Voor het Linux-scenario werd een opstelling voorzien met drie virtuele machines op basis van AlmaLinux 9. Deze omgeving is gebaseerd op een configuratie uit het vak Software Development Project (SDP) en werd gekozen omdat ze representatief is voor een typische Linux-opstelling binnen de opleiding.

De Linuxomgeving wordt zowel met RustProv als met Vagrant opgebouwd. Voor beide oplossingen worden dezelfde AlmaLinux9-images, IP-adressen, hardware-instellingen en provisioningscripts gebruikt. Hierdoor kan de werking van RustProv onder dezelfde omstandigheden worden vergeleken met de bestaande Vagrant-aanpak.

\subsection{Eén virtuele machine}

Als eerste Linux-testgeval wordt één afzonderlijke AlmaLinux9-VM gebruikt. Deze test heeft als doel te verifiëren of RustProv een enkele Linux-machine correct kan opstarten en provisionen. In tegenstelling tot de meerledige Linuxopstelling wordt in dit scenario een eenvoudig Bash-script gebruikt, zodat de basiswerking van de Linuxprovisioning afzonderlijk kan worden gecontroleerd.

\paragraph{Provisioningscript voor één Linux-VM}

Het script voert enkele eenvoudige opdrachten uit op de gastmachine. Eerst wordt een tekstbericht naar de console geschreven. Vervolgens wordt een bestand aangemaakt waarin een tweede tekstbericht wordt opgeslagen. Ten slotte wordt de inhoud van dit bestand opnieuw weergegeven op de console. Op basis van deze uitvoer kan worden gecontroleerd of het script effectief op de juiste virtuele machine werd uitgevoerd. De configuratie van dit scenario wordt weergegeven in Listing~\ref{lst:bplinux}.

\begin{lstlisting}[language=bash,caption={Eenvoudig provisioningscript voor één Linux-VM.},label={lst:bplinux}]
    #!/usr/bin/env bash
    echo "Hello, World!"
    echo "Hello, World from a file!" > ~/hello.txt
    cat ~/hello.txt
\end{lstlisting}

Voor het experiment met één Linux-VM werd dezelfde configuratie opgesteld voor RustProv en Vagrant. Beide gebruiken dezelfde AlmaLinux9-image, VM-naam, hardware-instellingen, netwerkadapter en het provisioningscript \texttt{00-hello.sh}. De configuraties worden weergegeven in Listing~\ref{lst:rustprov-linux-config-one} en Listing~\ref{lst:vagrant-linux-config-one}.

\begin{lstlisting}[language=bash,caption={RustProv-configuratie voor één Linux-VM.},label={lst:rustprov-linux-config-one}]
    [linuxbp]
    image = "almalinux9"
    cores = 4
    memory = 4096
    script = ["00-hello.sh"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
\end{lstlisting}


\begin{lstlisting}[language=ruby,caption={Vagrant-configuratie voor één Linux-VM},label={lst:vagrant-linux-config-one}]
    Vagrant.configure("2") do |config|
    config.vm.define "linuxbp" do |linux|
    linux.vm.box = "almalinux/9"
    linux.vm.box_version = "9.7.20260518"
    linux.vm.hostname = "linuxbp"
    linux.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller", auto_config: false
    linux.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    linux.vm.provision "shell", path: "scripts/00-hello.sh"
    end
    end
\end{lstlisting}



\subsection{Meerdere virtuele machines}

Daarnaast wordt ook een Linux-opstelling met drie virtuele machines getest. Deze configuratie sluit aan bij een SDP-omgeving waarin meerdere AlmaLinux9-systemen gelijktijdig worden ingezet. Het gebruik van meerdere Linux-VM's maakt het mogelijk om RustProv te evalueren in een scenario met meerdere nodes, afzonderlijke scripts en een gezamenlijke uitvoeringscontext.

De drie VM's hebben elk een eigen rol binnen de testomgeving. De eerste VM wordt gebruikt voor de database en Loki. De tweede VM bevat de WordPress-installatie en Promtail. De derde VM wordt gebruikt voor de monitoring met Grafana. De configuratie van deze VM's is onderling verbonden, omdat WordPress gebruikmaakt van de database op de eerste VM en Grafana de loggegevens uit Loki kan gebruiken.

Voor elke Linux-VM wordt een apart provisioningscript voorzien. Daardoor krijgt elke machine een eigen configuratie en rol binnen de bredere testomgeving. Deze aanpak maakt het mogelijk om te beoordelen of RustProv de juiste scriptstappen op de juiste machine uitvoert en of meerdere Linux-systemen correct volgens de gewenste volgorde kunnen worden verwerkt.

Dezelfde drie VM-rollen, scripts en netwerkadressen worden ook in de Vagrantconfiguratie opgenomen. Hierdoor worden de RustProv- en Vagrantopstelling inhoudelijk gelijk gehouden.

\newpage
\subsubsection{Linux-script 1: database en Loki}

Dit script installeert de databaseomgeving en Loki, zodat loggegevens centraal kunnen worden opgeslagen en later geanalyseerd. Het vormt de basislaag van de monitoringopstelling. De configuraties hiervan wordt weergegeven in Listing~\ref{lst:dbloki}

\begin{lstlisting}[language=bash,caption={Linux-script 1: installatie van database en Loki.},label={lst:dbloki}]
    #!/usr/bin/env bash
    set -euo pipefail
    
    INTERFACE="eth1"
    
    if nmcli connection show "${INTERFACE}" &>/dev/null; then
    nmcli connection modify "${INTERFACE}" \
    ipv4.addresses "192.168.0.10/24" \
    ipv4.gateway "192.168.0.1" \
    ipv4.dns "8.8.8.8,1.1.1.1" \
    ipv4.method "manual"
    else
    nmcli connection add type ethernet con-name "${INTERFACE}" ifname "${INTERFACE}" \
    ipv4.addresses "192.168.0.10/24" \
    ipv4.gateway "192.168.0.1" \
    ipv4.dns "8.8.8.8,1.1.1.1" \
    ipv4.method "manual"
    fi
    
    nmcli connection up "${INTERFACE}"
    sleep 5
    
    dnf update -y
    dnf install -y curl unzip mariadb-server policycoreutils-python-utils firewalld
    
    systemctl enable --now firewalld
    
    firewall-cmd --permanent --add-service=mysql
    firewall-cmd --permanent --add-port=3100/tcp
    firewall-cmd --reload
    
    if ! grep -q "^bind-address" /etc/my.cnf.d/mariadb-server.cnf 2>/dev/null; then
    sed -i '/\[mysqld\]/a bind-address = 0.0.0.0' /etc/my.cnf.d/mariadb-server.cnf
    else
    sed -i 's/^bind-address\s*=.*/bind-address = 0.0.0.0/' /etc/my.cnf.d/mariadb-server.cnf
    fi
    
    systemctl enable --now mariadb
    
    DB_NAME="wordpress"
    DB_USER="wp_user"
    DB_PASS="password2026!"
    WP_SERVER_IP="192.168.0.20"
    
    mysql -u root <<EOF
    CREATE DATABASE IF NOT EXISTS ${DB_NAME} DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
    CREATE USER IF NOT EXISTS '${DB_USER}'@'${WP_SERVER_IP}' IDENTIFIED BY '${DB_PASS}';
    GRANT ALL PRIVILEGES ON ${DB_NAME}.* TO '${DB_USER}'@'${WP_SERVER_IP}';
    FLUSH PRIVILEGES;
    EOF
    
    LOKI_VERSION="2.9.2"
    mkdir -p /etc/loki /var/loki
    
    curl -sSL -O "https://github.com/grafana/loki/releases/download/v${LOKI_VERSION}/loki-linux-amd64.zip"
    unzip -o loki-linux-amd64.zip
    mv loki-linux-amd64 /usr/local/bin/loki
    chmod +x /usr/local/bin/loki
    rm -f loki-linux-amd64.zip
    
    cat <<'EOF' > /etc/loki/loki-config.yml
    auth_enabled: false
    
    server:
    http_listen_port: 3100
    grpc_listen_port: 9096
    
    common:
    path_prefix: /var/loki
    storage:
    filesystem:
    chunks_directory: /var/loki/chunks
    rules_directory: /var/loki/rules
    replication_factor: 1
    ring:
    kvstore:
    store: inmemory
    
    schema_config:
    configs:
    - from: 2020-05-15
    store: boltdb-shipper
    object_store: filesystem
    schema: v11
    index:
    prefix: index_
    period: 24h
    
    ruler:
    alertmanager_url: http://localhost:9093
    EOF
    
    chcon -t bin_t /usr/local/bin/loki 2>/dev/null || true
    
    cat <<'EOF' > /etc/systemd/system/loki.service
    [Unit]
    Description=Loki service
    After=network.target
    
    [Service]
    Type=simple
    User=root
    ExecStart=/usr/local/bin/loki -config.file=/etc/loki/loki-config.yml
    Restart=on-failure
    
    [Install]
    WantedBy=multi-user.target
    EOF
    
    systemctl daemon-reload
    systemctl enable --now loki
\end{lstlisting}

\newpage
\subsubsection{Linux-script 2: WordPress en Promtail}

Dit script installeert WordPress en Promtail. WordPress levert de webtoepassing, terwijl Promtail de logs van het systeem doorstuurt naar Loki voor centrale logverzameling.De configuraties hiervan wordt weergegeven in Listing~\ref{lst:wppromtail}

\begin{lstlisting}[language=bash,caption={Linux-script 2: installatie van WordPress en Promtail.},label={lst:wppromtail}]
    #!/usr/bin/env bash
    set -euo pipefail
    
    INTERFACE="eth1"
    
    nmcli con modify "$INTERFACE" ipv4.addresses 192.168.0.20/24 ipv4.gateway 192.168.0.1 ipv4.dns "8.8.8.8 1.1.1.1" ipv4.method manual
    nmcli con up "$INTERFACE"
    sleep 5 
    
    
    dnf install -y epel-release
    dnf install -y nginx php-fpm php-mysqlnd php-cli php-curl php-gd php-mbstring php-xml php-intl php-zip curl unzip policycoreutils-python-utils firewalld tar
    
    systemctl enable --now firewalld
    firewall-cmd --permanent --add-service=http
    firewall-cmd --reload
    
    mkdir -p /var/www
    curl -sSL https://wordpress.org/latest.tar.gz | tar -xz -C /var/www/
    
    chown -R nginx:nginx /var/www/wordpress
    
    DB_HOST="192.168.0.10"
    DB_NAME="wordpress"
    DB_USER="wp_user"
    DB_PASS="password2026!"
    
    cat <<EOF > /var/www/wordpress/wp-config.php
    <?php
    define( 'DB_NAME', '${DB_NAME}' );
    define( 'DB_USER', '${DB_USER}' );
    define( 'DB_PASSWORD', '${DB_PASS}' );
    define( 'DB_HOST', '${DB_HOST}' );
    define( 'DB_CHARSET', 'utf8mb4' );
    define( 'DB_COLLATE', '' );
    
    $(curl -sSL https://api.wordpress.org/secret-key/1.1/salt/)
    
    \$table_prefix = 'wp_';
    define( 'WP_DEBUG', false );
    
    if ( ! defined( 'ABSPATH' ) ) {
        define( 'ABSPATH', __DIR__ . '/' );
    }
    require_once ABSPATH . 'wp-settings.php';
    EOF
    
    chown nginx:nginx /var/www/wordpress/wp-config.php
    
    chcon -Rt httpd_sys_rw_content_t /var/www/wordpress
    setsebool -P httpd_can_network_connect_db 1
    setsebool -P httpd_can_network_connect 1
    
    sed -i 's/user = apache/user = nginx/g' /etc/php-fpm.d/www.conf
    sed -i 's/group = apache/group = nginx/g' /etc/php-fpm.d/www.conf
    systemctl enable --now php-fpm
    
    mv /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak
    cat <<'EOF' > /etc/nginx/nginx.conf
    user nginx;
    worker_processes auto;
    error_log /var/log/nginx/error.log;
    pid /run/nginx.pid;
    include /usr/share/nginx/modules/*.conf;
    events { worker_connections 1024; }
    http {
        log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
        '$status $body_bytes_sent "$http_referer" '
        '"$http_user_agent" "$http_x_forwarded_for"';
        access_log  /var/log/nginx/access.log  main;
        sendfile            on;
        tcp_nopush          on;
        tcp_nodelay         on;
        keepalive_timeout   65;
        types_hash_max_size 4096;
        include             /etc/nginx/mime.types;
        default_type        application/octet-stream;
        include /etc/nginx/conf.d/*.conf;
    }
    EOF
    
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name _;
        root /var/www/wordpress;
        index index.php index.html index.htm;
        
        location / {
            try_files $uri $uri/ /index.php?$args;
        }
        
        location ~ \.php$ {
            try_files $uri =404;
            # AlmaLinux uses this socket path for PHP-FPM
            fastcgi_pass unix:/run/php-fpm/www.sock;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
        }
    }
    EOF
    
    nginx -t
    systemctl enable --now nginx
    
    PROMTAIL_VERSION="2.9.2"
    mkdir -p /etc/promtail /var/promtail
    
    curl -sSL -O "https://github.com/grafana/loki/releases/download/v${PROMTAIL_VERSION}/promtail-linux-amd64.zip"
    unzip -o promtail-linux-amd64.zip
    mv promtail-linux-amd64 /usr/local/bin/promtail
    chmod +x /usr/local/bin/promtail
    rm -f promtail-linux-amd64.zip
    
    LOKI_IP="192.168.0.10"
    
    cat <<EOF > /etc/promtail/promtail-config.yml
    server:
    http_listen_port: 9080
    grpc_listen_port: 0
    
    positions:
    filename: /var/promtail/positions.yaml
    
    clients:
    - url: http://${LOKI_IP}:3100/loki/api/v1/push
    
    scrape_configs:
    - job_name: nginx
    static_configs:
    - targets:
    - localhost
    labels:
    job: nginx_access
    host: wordpress-server
    __path__: /var/log/nginx/access.log
    - targets:
    - localhost
    labels:
    job: nginx_error
    host: wordpress-server
    __path__: /var/log/nginx/error.log
    EOF
    
    cat <<'EOF' > /etc/systemd/system/promtail.service
    [Unit]
    Description=Promtail service
    After=network.target
    
    [Service]
    Type=simple
    User=root
    ExecStart=/usr/local/bin/promtail -config.file=/etc/promtail/promtail-config.yml
    Restart=on-failure
    
    [Install]
    WantedBy=multi-user.target
    EOF
    
    systemctl daemon-reload
    systemctl enable --now promtail
\end{lstlisting}

\newpage
\subsubsection{Linux-script 3: monitoring met Grafana}

Dit script installeert Grafana en maakt de monitoringomgeving volledig. Grafana gebruikt de data uit Loki om dashboards en logoverzichten weer te geven. De configuraties hiervan wordt weergegeven in Listing~\ref{lst:mgrafana}

\begin{lstlisting}[language=bash,caption={Linux-script 3: installatie van Grafana en monitoring.},label={lst:mgrafana}]
    #!/usr/bin/env bash
    set -euo pipefail
    
    INTERFACE="eth1"
    
    if nmcli con show "${INTERFACE}" >/dev/null 2>&1; then
    nmcli con mod "${INTERFACE}" ipv4.addresses 192.168.0.30/24 ipv4.gateway 192.168.0.1 ipv4.dns "8.8.8.8 1.1.1.1" ipv4.method manual
    else
    nmcli con add type ethernet con-name "${INTERFACE}" ifname "${INTERFACE}" ipv4.addresses 192.168.0.30/24 ipv4.gateway 192.168.0.1 ipv4.dns "8.8.8.8 1.1.1.1" ipv4.method manual
    fi
    
    nmcli con up "${INTERFACE}"
    sleep 5
    
    dnf install -y wget curl unzip firewalld
    
    systemctl enable --now firewalld
    firewall-cmd --permanent --add-port=3000/tcp
    firewall-cmd --reload
    
    # Create the RPM repository file for Grafana
    cat <<EOF > /etc/yum.repos.d/grafana.repo
    [grafana]
    name=grafana
    baseurl=https://rpm.grafana.com
    repo_gpgcheck=1
    enabled=1
    gpgcheck=1
    gpgkey=https://rpm.grafana.com/gpg.key
    sslverify=1
    sslcacert=/etc/pki/tls/certs/ca-bundle.crt
    EOF
    
    dnf install -y grafana
    
    LOKI_IP="192.168.0.10"
    mkdir -p /etc/grafana/provisioning/datasources
    
    cat <<EOF > /etc/grafana/provisioning/datasources/loki.yaml
    apiVersion: 1
    
    datasources:
    - name: Loki
    type: loki
    access: proxy
    url: http://${LOKI_IP}:3100
    isDefault: true
    editable: true
    EOF
    
    PROMTAIL_VERSION="2.9.2"
    mkdir -p /etc/promtail /var/promtail
    
    curl -sSL -O "https://github.com/grafana/loki/releases/download/v${PROMTAIL_VERSION}/promtail-linux-amd64.zip"
    unzip -o promtail-linux-amd64.zip
    mv promtail-linux-amd64 /usr/local/bin/promtail
    chmod +x /usr/local/bin/promtail
    rm -f promtail-linux-amd64.zip
    
    cat <<EOF > /etc/promtail/promtail-config.yml
    server:
    http_listen_port: 9080
    grpc_listen_port: 0
    
    positions:
    filename: /var/promtail/positions.yaml
    
    clients:
    - url: http://${LOKI_IP}:3100/loki/api/v1/push
    
    scrape_configs:
    - job_name: varlogs
    static_configs:
    - targets:
    - localhost
    labels:
    job: varlogs
    host: monitoring-server
    __path__: /var/log/*.log
    EOF
    
    cat <<'EOF' > /etc/systemd/system/promtail.service
    [Unit]
    Description=Promtail service
    After=network.target
    
    [Service]
    Type=simple
    User=root
    ExecStart=/usr/local/bin/promtail -config.file=/etc/promtail/promtail-config.yml
    Restart=on-failure
    
    [Install]
    WantedBy=multi-user.target
    EOF
    
    systemctl daemon-reload
    systemctl enable --now grafana-server
    systemctl enable --now promtail
\end{lstlisting}
\newpage


Voor de Linuxexperimenten werd dezelfde VM-opstelling beschreven in zowel een
TOML-configuratiebestand voor RustProv als een \texttt{Vagrantfile} voor Vagrant.
Op die manier wordt ervoor gezorgd dat beide provisioners dezelfde virtuele
machines, hardware-instellingen, netwerkadapter en provisioningscripts gebruiken.
De configuraties worden weergegeven in Listing~\ref{lst:rustprov-linux-config} en
Listing~\ref{lst:vagrant-linux-config}.


\begin{lstlisting}[language=bash,caption={RustProv-configuratie voor meerdere Linux-VM's.},label={lst:rustprov-linux-config}]
    [dbloki]
    image = "almalinux9"
    cores = 4
    memory = 4096
    script = ["01-db-loki.sh"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
    
    [wppromtail]
    image = "almalinux9"
    cores = 4
    memory = 4096
    script = ["02-wordpress-promtail.sh"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
    
    [monitorgrafana]
    image = "almalinux9"
    cores = 4
    memory = 4096
    script = ["03-monitoring-grafana.sh"]
    bridgeNetwork = "Realtek Gaming 2.5GbE Family Controller"
\end{lstlisting}
\newpage




\begin{lstlisting}[language=ruby,caption={Vagrant-configuratie voor meerdere Linux-VM's.},label={lst:vagrant-linux-config}]
    Vagrant.configure("2") do |config|
    config.vm.box = "almalinux/9"
    config.vm.box_version = "9.7.20260518"
    config.vm.define "dbloki" do |dbloki|
    dbloki.vm.hostname = "dbloki"  
    dbloki.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller", auto_config: false   
    dbloki.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    dbloki.vm.provision "shell", path: "scripts/01-db-loki.sh"
    end
    config.vm.define "wppromtail" do |wp|
    wp.vm.hostname = "wppromtail"
    wp.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller", auto_config: false
    wp.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    wp.vm.provision "shell", path: "scripts/02-wordpress-promtail.sh"
    end
    config.vm.define "monitorgrafana" do |grafana|
    grafana.vm.hostname = "monitorgrafana"   
    grafana.vm.network "public_network", bridge: "Realtek Gaming 2.5GbE Family Controller", auto_config: false  
    grafana.vm.provider "virtualbox" do |vb|
    vb.memory = 4096
    vb.cpus = 4
    end
    grafana.vm.provision "shell", path: "scripts/03-monitoring-grafana.sh"
    end
    end
\end{lstlisting}